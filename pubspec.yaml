name: handle_event_change
description: A new Flutter project.
version: 0.0.1
homepage: https://github.com/dev-khanh/handle_event_change

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  plugin:
    platforms:
      android:
        package: com.devk.handle_event_change
        pluginClass: HandleEventChangePlugin
      ios:
        pluginClass: HandleEventChangePlugin